---------------------------- DAY 1 --------------------------------------------------

Java is Case Sensitive language.
Identifier is a user-defined name given to variables, constant, class, method, packages, interface.
Rules:-
	A keyword can't be a identifier.
	Identifier name can't contain space, special character's.
	Identifier cannot start with digit.
	Identifier can be the combination of alphabet's, digits, underscore.
	Identifier can be of any length.
Naming Conventions:-
Class:-
	First letter of the class name should be in uppercase. 
	If class consists of more than one word first letter of each word should be capital.

		 JDK-> javac and JRE->JVM

If you make source code change you must recompile the source code.
.class file is created only if there are no syntactical errors in source code.
System and string are both built in classes offered by java

Variable is named container in memory which can store a value and the value can be change(value can vary)
To declare a variable in java use following syntax:-  data_type nameofVariables
Java offers following 8 primitive datatypes they are as follows
-byte		-float		-char 			-boolean
-short		-double		
-int		value with  	a single char  		true or false
-long		decimal value   in single quotes
values without   
decimal value	 


